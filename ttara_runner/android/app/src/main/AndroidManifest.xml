<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- 02_app_code/README.md 권한표(04_store_listing.md 04번 문서와 동일) 반영 -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- 2026-08-08: RECORD_AUDIO 제거 — 앱은 어디에서도 마이크 오디오를 실제로 녹음/분석하지 않는다.
         2026-08-24: READ_MEDIA_AUDIO/READ_MEDIA_VIDEO도 제거 — 실제 파일 선택은 file_picker가
         Android SAF(시스템 파일 선택 도구)로 처리해 이 권한들이 애초에 전혀 필요 없었고,
         Google Play "사진 및 동영상 권한 정책"(READ_MEDIA_IMAGES/VIDEO 대신 시스템 선택 도구를
         쓰라는 정책) 위반으로 업데이트 심사가 거절됐다. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"/>
    <!-- 2026-08-11: 실제 Google Play Billing 연동(광고 제거 인앱결제)에 필요.
         Billing 라이브러리 자체 매니페스트에도 포함돼 있어 보통 자동 병합되지만,
         명시적으로 선언해 누락 위험을 없앤다. -->
    <uses-permission android:name="com.android.vending.BILLING"/>
    <!-- 2026-08-12: flutter_local_notifications 16+는 예약 알림을 실제로 처리할 리시버를
         플러그인 자체 매니페스트에 더 이상 넣어주지 않는다 — 앱 쪽에서 직접 선언해야
         한다(공식 README "AndroidManifest.xml setup" 항목). 이게 빠져 있어서 알람은
         정확히 울렸는데도 실제 알림을 만드는 컴포넌트가 아예 등록이 안 돼 있어 조용히
         아무 일도 안 일어나던 게 "학습 리마인더가 안 울려요" 버그의 진짜 원인이었다.
         재부팅 후 재예약을 위한 BOOT_COMPLETED 권한도 함께 추가. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <!-- 2026-08-13: inexactAllowWhileIdle로 예약하던 게 진짜 원인 — Doze 모드에서
         OS가 알람을 배치 처리 창까지 미뤄버려서, 앱을 열어 프로세스를 깨우기 전엔
         정시에 안 울리던 증상(11:50 예약 → 안 울림 → 앱 켠 12:10에야 발동)이 이걸로
         설명된다. exactAllowWhileIdle로 바꾸려면 이 권한이 필요하다(Android 12+). -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>

    <!-- 2026-08-12: Android Auto Backup이 재설치 시 flutter_local_notifications의
         구버전 예약 알림 캐시(SharedPreferences)를 복원하면서, 현재 플러그인 버전의
         Gson TypeToken과 형식이 안 맞아 "Missing type parameter" RuntimeException으로
         알림 켜기가 죽는 문제 발견 — 백업/복원 자체를 꺼서 스테일 캐시 복원을 막는다. -->
    <application
        android:label="쉐도잉랩"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:fullBackupContent="false">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- AdMob 앱 ID. 2026-08-11: YBG Ltd 실제 AdMob 계정의 프로덕션 앱 ID로 교체 완료. -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3703514883602304~8606315538" />

        <!-- just_audio_background 필수 서비스 선언 — 이게 없으면 JustAudioBackground.init()이
             MediaBrowserService 연결을 영원히 기다려 앱이 스플래시 화면에서 멈춘다. -->
        <service android:name="com.ryanheise.audioservice.AudioService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="true" tools:ignore="Instantiatable">
            <intent-filter>
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
        </service>

        <receiver android:name="com.ryanheise.audioservice.MediaButtonReceiver"
            android:exported="true" tools:ignore="Instantiatable">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>

        <!-- 2026-08-12: flutter_local_notifications 공식 필수 설정 — 이게 없으면 예약
             알림의 AlarmManager 브로드캐스트가 전달될 대상 자체가 없어 조용히 무시된다. -->
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
