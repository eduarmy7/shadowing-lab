# UX/UI 설계 문서 — 쉐도잉랩(ShadowingLab, 구 따라/TTARA): 쉐도잉 영어 학습 앱

> 작성: ux-designer · 대상 플랫폼: iOS + Android (Flutter) · 기준일: 2026-08-04 (2026-08-08 앱 분리 반영 개정)
> 2026-08-04 사용자 요청으로 앱 이름이 "따라(TTARA)"에서 "쉐도잉랩(ShadowingLab)"으로 변경됐다(코드의 `AppConstants.appName`이 실제 기준). 아래 본문의 "따라"/"TTARA" 브랜드명 표기는 개명 전 초안이 남아있던 것으로, 이번에 일괄 정정한다.
> **2026-08-08 제품 분리(중요)**: 쉐도잉랩이 두 개의 별도 앱으로 분리됐다. (1) **쉐도잉랩(ShadowingLab, 본 문서)** — 라이브러리(CNN/BBC 뉴스·대화 콘텐츠) 탭과 PRO 구독/Paywall 관련 기능이 코드에서 전부 삭제되고, **홈 + 마이 2탭 구조**만 남았다. 수익모델도 정기구독에서 **단발성 인앱결제(IAP) "광고 제거", 14,900원**으로 전환됐다(배너·전면 광고를 영구 제거). (2) **English News Shadowing Lab** — 옛 라이브러리/구독 코드를 그대로 이어받은 별도 앱으로, 별도 UX 설계 문서(`_workspace/news_shadowing_lab/`)에서 관리된다(본 문서의 범위 아님). 아래 본문 중 화면 #7(라이브러리 홈)·#8(콘텐츠 상세)은 삭제 표시했고, #9(구독 결제)는 신규 "광고 제거 구매 시트"로 대체했다. 홈 탭(#0~#6)과 마이 탭의 나머지 화면(#10~#12) 구성 자체는 이번 분리로 바뀌지 않았다.

---

## 앱 개요

| 항목 | 내용 |
|---|---|
| **앱 이름** | **쉐도잉랩 (ShadowingLab)** — 2026-08-04 사용자 요청으로 "따라(TTARA)"에서 개명(원래 안은 "따라 하다"의 '따라'에서 착안한 브랜드명이었음, 참고용으로만 남김). 스토어 부제: *"자막 있어도 없어도, 진짜 쉐도잉"* |
| **플랫폼** | 크로스플랫폼 (Flutter) — iOS HIG + Material Design 3 동시 반영 |
| **핵심 사용자 페르소나 1** | **직장인 자기계발러 (28~38세)** — 출퇴근길/점심시간에 짬 시간 학습. 미드/뉴스/유튜브에서 저장한 영상으로 혼자 연습하고 싶으나, 기존 앱들이 너무 복잡해 3일 만에 삭제한 경험 있음. "예쁘고 쉬운 앱"에 값을 지불할 의향 있음 → 광고 제거(유료 전환) 타깃. |
| **핵심 사용자 페르소나 2** | **취준생/어학시험 준비생 (20~27세)** — 스크립트가 있는 강의/인터뷰 영상을 통째로 넣고 문장 단위로 반복 훈련. 무료 기능만으로도 매일 사용, 광고에는 관대하지만 학습 흐름이 끊기면 이탈. |
| **핵심 가치 제안** | 내가 가진 아무 영상/음성이나 넣으면 — 자막이 있으면 자막 그대로, 없으면 무음 구간을 기준으로 — 자동으로 문장 단위로 나눠주고, 원어민처럼 말할 때까지 손 안 대고 반복시켜주는 가장 미니멀한 쉐도잉 앱. (2026-08-05: 문장 분리는 서버 AI 음성인식이 아닌 **기기 내 로컬 처리**로만 수행 — 사용자 파일이 외부로 전송되지 않으며, 무료 사용자가 늘어도 개발자의 API 비용이 발생하지 않는 구조) |

---

## 경쟁 앱 분석 및 차별화 전략

### 참고 앱 조사 요약 — "쉐도잉 플레이어: 언어 학습" (Google Play / App Store)

스토어 페이지·설명·리뷰 검색으로 확인한 참고 앱의 실제 기능 목록:

> 자막 동기화 표시, 자막 탭-이동 재생, AI 자막 번역/SRT 생성, 구간반복+Listen&Repeat 모드, **0.01 단위 재생속도 조절**, 자막별 반복횟수 설정, 자막 북마크, **SRT 자막 직접 편집**, 플레이리스트+자동연속재생, 이중자막 동시표시, 온라인 사전 링크, Google Translate/ChatGPT 연동, 가로모드

**진단**: 기능 하나하나는 훌륭하지만, 이 모든 기능이 **재생 화면 하나에 동시에 노출**되는 구조로 추정된다. "복잡하고 세련되지 않다"는 사용자 불만은 특정 기능이 나빠서가 아니라, 아래 4가지 UX 구조적 문제에서 비롯된다고 판단한다.

| # | 추정 문제 | 근거 |
|---|---|---|
| 1 | **화면당 컨트롤 과다 (기능 나열형 설계)** | 자막 목록 패널 + 재생 컨트롤 + 속도 슬라이더 + 반복설정 + 북마크 + 사전 + 번역 + SRT편집 + 플레이리스트가 한 화면에서 동시에 도달 가능 → 학습에 필요한 것(재생/반복)과 부가 기능(편집/사전)이 같은 시각적 무게로 노출 |
2 | **시각적 위계 부재** | 모든 아이콘/버튼이 비슷한 크기·색으로 나열되는 유틸리티 앱 특유의 구성 → 지금 눌러야 할 "가장 중요한 버튼"이 무엇인지 즉시 알기 어려움 |
| 3 | **온보딩 부재 + 수동 자막 방식의 진입장벽** | SRT 파일 개념, 타임스탬프 편집을 사용자가 알아야 시작 가능 → 처음 켠 화면이 곧 가장 복잡한 화면 |
| 4 | **무료/프리미엄 경계 모호** | 인앱결제로 기능 단위 잠금 방식 추정 → 어디까지가 무료인지 탐색해야 알 수 있음, 신뢰감 저하 |

### 개선 전략 (Before → After)

| 영역 | Before (참고 앱 추정) | After (쉐도잉랩 설계) |
|---|---|---|
| 학습 화면 밀도 | 재생/자막목록/속도/북마크/사전/번역/편집 버튼 10개 이상이 한 화면에 상시 노출 | 학습 화면엔 **문장 카드 + 재생 버튼 + 반복 도트 + 마이크 아이콘 4요소만** 상시 노출. 속도조절·북마크·사전 등은 하단 시트(2차 진입)로 격리 |
| 문장 분리 방식 | 사용자가 SRT를 직접 만들거나 편집 (타임스탬프 개념 필요) | **로컬 자동 분리(무음 감지 또는 자막 매칭, 서버 AI 호출 없음)** → 결과를 카드 리스트로 보여주고, 틀린 곳만 드래그 한 번으로 미세조정 (타임스탬프 개념 노출 안 함, 음성 파일은 텍스트 없이 순서만 표시) |
| 첫 실행 경험 | 온보딩 없이 곧바로 복잡한 플레이어 화면 진입 (스토어 설명에 기능 나열) | 3단계 온보딩(가치제안 → 업로드 시연 → 첫 문장 쉐도잉 체험)으로 "손대지 않아도 되는" 첫인상 제공 |
| 시각적 위계 | 아이콘/버튼 크기·색이 균일, 우선순위 불명확 | Primary 행동은 큰 Filled 버튼 1개만, 나머지는 톤 낮은 아이콘/텍스트 버튼으로 축소. 컬러 대비로 "지금 할 일"을 항상 1개로 좁힘 |
| 무료 ↔ 유료 경계 | 인앱결제 기능 단위 잠금(경계 불명확 추정) | **기능 자체는 전부 무료, 광고 유무만 유료 전환 대상** — 단발성 IAP "광고 제거"(14,900원)로 배너·전면 광고만 없앨 뿐 학습 기능 잠금은 없음(2026-08-08 변경. 과거 초안에 있던 "탭으로 물리적 분리(홈 무료 vs 라이브러리 PRO)" 구조는 라이브러리 탭 자체가 별도 앱으로 분리되며 폐기) |
| 반복 학습 마찰 | 문장마다 재생/반복 버튼을 수동 조작 | **자동 반복재생 + Hands-free 모드** — 화면을 한 번 켜두면 탭 없이 다음 문장까지 진행 |
| 정보 밀도 대비 여백 | 다수 텍스트/아이콘이 촘촘히 배치 (추정) | 8dp 그리드 기반 여백 시스템 + 카드형 레이아웃으로 "숨 쉬는 화면" 구현 |

---

## 사용자 여정 맵

| 단계 | 사용자 행동 | 화면 | 감정 | 기회 |
|---|---|---|---|---|
| 발견 | 스토어에서 검색, 스크린샷/리뷰 확인 후 설치 | (스토어) | 기대 반 의심 반 | 스크린샷에서 "미니멀함"이 한눈에 보여야 함 |
| 첫 실행 | 온보딩 3장 스와이프, 마이크/파일 권한 허용 | 온보딩 | 호기심 | 30초 내 가치 체감 — 실제 앱 UI 미리보기로 신뢰 확보 |
| 첫 업로드 | 음성만 있는 파일 또는 영상+자막 파일(2개)을 선택 | 파일 업로드 | 약간의 불안("잘 될까?") | 두 경로(자막 유무)를 업로드 단계에서부터 명확히 구분해 "이후 결과가 왜 다른지" 미리 이해시킴 |
| 로컬 분리 대기 (음성만 업로드 시) | 무음 구간 분석이 끝날 때까지 잠깐 대기 (영상+자막은 즉시 완료돼 이 단계 생략) | 분리 중(로컬 처리) | 짧은 기다림, 안심 | "내 기기에서만 처리되고 있다"는 문구로 프라이버시 신뢰 확보, 네트워크 대기가 아님을 시각적으로 구분 |
| 결과 확인 | (자막 있음) 자막 문장 목록 스크롤 확인 / (자막 없음) 순서 번호만 있는 구간 목록 확인, 필요 시 경계만 미세조정 | 문장 분리 결과 확인 | 통제감 · (자막 없는 경우) 약간의 낯섦 | 자막 없는 경우 "왜 텍스트가 없는지"를 결과 화면 진입 즉시 안내해 혼란 제거 |
| 핵심 학습 | 문장 듣기 → 따라 말하기 반복 → 다음 문장 | 쉐도잉 학습 (핵심 루프) | 몰입 | 손 안 대고 진행되는 리듬, 완료 시 작은 보상감(도파민) |
| 세션 종료 | 학습한 문장 수/시간 확인 | 학습 완료 요약 | 성취감 | 스트릭(연속학습일) 강조로 재방문 유도 |
| 광고 제거 전환 (2026-08-08 갱신) | 마이 탭에서 "광고 제거" 배너 탭 → 구매 시트 확인 | 광고 제거 구매 시트 | 신중 | 배너/전면 광고 각각을 제거한다는 가치를 2~3개 불릿으로 간결히 증명, 부담 없는 단발성 가격(14,900원)으로 구독 대비 결정 마찰 최소화 |
| 습관화 | 매일 스트릭 확인, 설정 조정 | 마이(통계/설정) | 자부심 | 캘린더 히트맵으로 꾸준함을 눈에 보이게 |

---

## 정보구조(IA) 및 네비게이션 구조

- **네비게이션 패턴**: Bottom Tab Bar (2탭) + Stack (탭별 계층 이동) + Modal/Bottom Sheet (편집·설정·광고 제거 구매류)
- **2026-08-08 변경 — 3탭 → 2탭**: 라이브러리 탭(CNN/BBC 큐레이션 콘텐츠 + PRO 구독)이 별도 앱("English News Shadowing Lab")으로 통째로 분리되며 이 앱에서 완전히 제거됐다. 남은 두 탭(홈/마이)만으로도 "화면당 기능 나열을 피한다"는 원래 IA 설계 원칙은 그대로 유지된다 — 오히려 탭이 줄어 구조는 더 단순해졌다.
- **수익모델 변경**: 정기구독(Paywall) → **단발성 IAP "광고 제거"(14,900원, SKU 1개)**. 배너 광고(홈 탭에서만 노출)와 전면 광고(세션 종료 화면 진입 시에만 노출)를 구매 즉시 영구 제거한다. 학습 기능 자체는 애초에 전부 무료이며 잠금 대상이 아니다.

```
쉐도잉랩 (2탭)
├─ Tab 1. 홈 (내 콘텐츠, 학습 기능 전부 무료)
│   ├─ 홈 (파일 목록: Empty / 목록, 하단 배너 광고 — 광고 제거 구매 시 미노출)
│   ├─ 파일 업로드 (음성만 / 영상+자막 선택 → 업로드 진행)
│   ├─ 분리 중 (로컬 처리 · 음성만 업로드 시에만 표시, 자막 매칭은 즉시 완료)
│   ├─ 문장 분리 결과 확인 (자막모드: 텍스트+타임스탬프 / 무자막모드: 순서번호+파형만)
│   ├─ 쉐도잉 학습 (핵심 화면)
│   │    └─ 학습 옵션 시트 (속도/반복횟수/자막표시 — 무자막 파일은 자막표시 항목 비활성)
│   └─ 학습 완료 요약 (세션 종료 시점 전면 광고 — 광고 제거 구매 시 미노출)
│
└─ Tab 2. 마이 (통계 · 설정 · 광고 제거 구매)
    ├─ 마이 홈 (스트릭 요약, 광고 제거 구매 상태/CTA 카드)
    ├─ 학습 기록 상세 (캘린더 히트맵)
    ├─ 광고 제거 구매 시트 (Bottom Sheet — 단발성 IAP 14,900원, 구매/복원)
    └─ 설정
         ├─ 학습 기본값 (기본 반복횟수, 기본 배속)
         ├─ 알림 설정
         ├─ 화면/다크모드
         ├─ 계정
         └─ 고객지원/약관/버전정보

+ 최초 1회: 온보딩 (탭 바 밖, 풀스크린 스택)

※ (참고, 이 앱 범위 아님) 옛 "Tab 2. 라이브러리(PRO · CNN/BBC 큐레이션)"는 별도 앱 English News
   Shadowing Lab로 그대로 이전됐다 — 해당 앱의 IA/화면 설계는 `_workspace/news_shadowing_lab/`
   문서에서 관리한다.
```

**깊이 규칙**: 홈 → 파일선택 → 분석 → 편집 → 학습 (최대 4 depth). 모든 화면에서 상단 좌측 Back, 학습 화면에서는 "종료" 버튼으로 언제든 홈 복귀 가능. 탭 전환은 항상 1탭으로 즉시 가능(스택 유지 — 탭 재진입 시 마지막 위치 기억).

---

## 화면 목록 총괄표

| # | 화면명 | 탭/진입경로 | 우선순위 | 스토어 스크린샷 후보 |
|---|---|---|---|---|
| 0 | 온보딩 | 최초 실행 | 필수 | ★ |
| 1 | 홈 (Empty/목록) | Tab 1 | 필수 | |
| 2 | 파일 업로드 (음성만 / 영상+자막) | Tab 1 → + | 필수 | |
| 3 | 분리 중 (로컬 처리, 음성만 업로드 시) | 업로드 후 자동 | 필수 | |
| 4 | 문장 분리 결과 확인 (자막모드/무자막모드) | 분리 완료 후 자동 | 필수 (차별화 핵심) | ★★★ |
| 5 | 쉐도잉 학습 화면 | 문장 확정 후 | **최핵심** | ★★★ |
| 6 | 학습 완료 요약 | 세션 종료 | 필수 | |
| 7 | ~~라이브러리 홈~~ (2026-08-08 삭제 — 별도 앱으로 이전) | — | — | — |
| 8 | ~~콘텐츠 상세/잠금~~ (2026-08-08 삭제 — 별도 앱으로 이전) | — | — | — |
| 9 | 광고 제거 구매 시트 (2026-08-08 신규 — 옛 구독 결제/Paywall 대체) | 마이 홈 배너 → 시트 | 필수 | |
| 10 | 마이 홈 | Tab 2 | 필수 | ★★ |
| 11 | 학습 기록 상세 | 마이 → 기록 | 권장 | |
| 12 | 설정 | 마이 → 설정 | 필수 | |

---

## 화면별 와이어프레임

### 0. 온보딩 — 첫인상, "복잡하지 않다"는 약속

- **레이아웃**: 풀스크린 3-슬라이드 스와이프 + 페이지 인디케이터, 마지막 슬라이드에 권한 요청 결합
- **핵심 컴포넌트**: 일러스트/모션 영역(상단 60%), 헤드라인, 서브텍스트, 페이지 도트, Skip 텍스트버튼(우상단), Primary CTA(하단 고정)
- **인터랙션**: 좌우 스와이프, 마지막 장에서 "시작하기" 탭 시 파일/마이크 접근 권한 시스템 다이얼로그
- **상태 처리**: Empty(해당없음) / Loading(해당없음) / Error(권한 거부 시 → 안내 배너로 "나중에 설정에서 허용 가능" 안내 후 진행 허용) / Success(홈 진입) / Partial(권한 일부만 허용 시 기능 제한 안내)

```
┌─────────────────────────┐
│                   건너뛰기│
│                          │
│      [ 파형 일러스트 ]    │
│      (재생→따라말하기     │
│       모션 애니메이션)     │
│                          │
│   아무 영상이나 넣으면     │
│   자동으로 문장이 나뉘어요  │
│                          │
│  타임스탬프? 몰라도 돼요.  │
│  자막 있으면 자막대로,     │
│  없으면 쉬는 구간마다       │
│  자동으로 나눠드려요.       │
│                          │
│        ● ○ ○            │
│  ┌────────────────────┐ │
│  │      다음           │ │
│  └────────────────────┘ │
└─────────────────────────┘
슬라이드 2: "듣고, 따라 말하고, 자동으로 다음 문장" (핵심 루프 시연 GIF)
슬라이드 3: "CNN·BBC 공식 자막으로, 오늘의 뉴스도 매일 새롭게" + 파일/마이크 권한 요청 → [시작하기]
```

---

### 1. 홈 — 내 콘텐츠 (무료)

**Empty 상태**
```
┌─────────────────────────┐
│  따라            ⚙︎      │  ← 타이틀만, 아이콘 최소화
│                          │
│                          │
│        🎧 (일러스트)      │
│   아직 학습할 파일이       │
│   없어요                  │
│  가진 영상/음성을          │
│  넣어서 시작해보세요        │
│                          │
│  ┌────────────────────┐ │
│  │  + 파일 불러오기      │ │
│  └────────────────────┘ │
│                          │
│ [배너 광고 영역 — 하단고정]│
├─────────────────┬─────────────────┤
│      🏠 홈      │      👤 마이      │
└─────────────────┴─────────────────┘
```

**목록 상태 (Populated)**
```
┌─────────────────────────┐
│  따라                    │
│  🔥 3일째 학습 중          │  ← 스트릭 미니배지(있을 때만)
│                          │
│  ┌────────────────────┐ │
│  │  + 새 파일 불러오기   │ │  ← 항상 최상단 고정 Primary
│  └────────────────────┘ │
│                          │
│  최근 학습                │
│  ┌────────────────────┐ │
│  │▶ BBC 인터뷰.mp4      │ │
│  │  문장 24개 · 62% 완료│ │  ← 진행률 바(얇게)
│  │  ▓▓▓▓▓▓▓▓▓░░░░      │ │
│  └────────────────────┘ │
│  ┌────────────────────┐ │
│  │▶ Friends_S1E3.mp3   │ │
│  │  문장 41개 · 완료     │ │
│  └────────────────────┘ │
│  ⋮ (스와이프 → 삭제/이름변경)│
│                          │
│ [배너 광고 영역]           │
├─────────────────┬─────────────────┤
│      🏠 홈      │      👤 마이      │
└─────────────────┴─────────────────┘
```
- **핵심 컴포넌트**: 스트릭 배지(옵션), Primary 업로드 버튼(항상 최상단 고정), 파일 카드(진행률 바 포함), 하단 배너 광고, 탭바
- **인터랙션**: 카드 탭 → 마지막 학습 지점부터 쉐도잉 화면 이어서 진입 / 좌스와이프 → 삭제·이름변경 / Pull-to-refresh 없음(로컬 데이터라 불필요)
- **상태 처리**: Empty(위 그림) / Loading(카드 자리 스켈레톤 3개) / Error(해당없음, 로컬 저장이라 거의 발생 안함) / Success(목록 표시) / Partial(일부 파일 분석 실패 시 카드에 "재분석 필요" 배지)
- **광고 배치 원칙**: 하단 배너는 탭바 바로 위, 콘텐츠 카드와 명확한 여백(16dp)으로 분리해 오탭 방지. 학습 화면(#5) 내부에는 배너를 절대 노출하지 않음 — 몰입 보호가 최우선. 배너는 **홈 탭에서만** 노출되며(마이 탭에는 애초에 없음), 세션 종료(#6) 화면의 전면 광고와 함께 마이 탭의 **광고 제거 구매(단발성 IAP, 14,900원, #9)** 완료 시 둘 다 영구적으로 사라진다.

---

### 2. 파일 업로드 — 음성만 vs 영상+자막, 두 경로를 입구에서부터 분리

**설계 원칙(2026-08-05 변경)**: 이 화면의 선택이 곧 4번 화면의 "텍스트 있음/없음" 모드를 결정한다. 참고 앱처럼 사용자가 SRT를 직접 만들 필요는 없지만, "자막을 붙일지 말지"는 사용자가 업로드 시점에 스스로 선택하는 정보이므로 여기서 명확히 갈래를 나눠 보여준다. 서버로 파일이 전송되지 않으므로 "업로드"라는 표현 대신 실제로는 "기기에 불러오기"에 가깝다 — 화면 타이틀은 "파일 불러오기"를 유지하되 진행 화면 문구에서 네트워크 전송 뉘앙스를 제거한다.

```
┌─────────────────────────┐
│ ← 파일 불러오기            │
│                          │
│  어떤 파일을 넣을까요?      │
│                          │
│  ┌────────────────────┐ │
│  │ 🎧 음성 파일만        │ │
│  │  mp3 · m4a · wav    │ │
│  │  텍스트 없이 듣고      │ │
│  │  따라 말하기 모드      │ │
│  └────────────────────┘ │
│  ┌────────────────────┐ │
│  │ 🎬 영상 + 자막 파일    │ │
│  │  mp4/mov + srt/vtt   │ │
│  │  자막 텍스트를 보면서   │ │
│  │  따라 말하기 모드      │ │
│  └────────────────────┘ │
│  ┌────────────────────┐ │
│  │ 🎙 방금 녹음한 음성    │ │
│  └────────────────────┘ │
│  최대 60분 · 서버 전송 없이 │
│  이 기기에서만 처리돼요     │
└─────────────────────────┘

"영상 + 자막 파일" 선택 시 — 2단계 선택:
┌─────────────────────────┐
│ ← 영상 파일 선택           │
│  📁 BBC_interview.mp4    │
│                          │
│  ✓ 같은 폴더에서 이름이     │
│    같은 자막 파일을 찾았어요 │
│  ┌────────────────────┐ │
│  │ 📝 BBC_interview.srt │ │
│  │   이 자막을 사용할까요?│ │
│  └────────────────────┘ │
│  [ 다른 자막 파일 선택 ]   │
│  [ 자막 없이 진행 ]        │  ← 선택 시 음성만 모드로 전환
└─────────────────────────┘

파일 선택 후 진행:
┌─────────────────────────┐
│  BBC_interview.mp4        │
│  불러오는 중                │
│                          │
│      ◔  68%              │
│   기기에서 불러오는 중이에요 │
│  ┌────────────────────┐ │
│  │      취소            │ │
│  └────────────────────┘ │
└─────────────────────────┘
```
- **핵심 컴포넌트**: 경로 선택 카드 2종(음성만 / 영상+자막) + 방금 녹음 카드, OS 파일 피커 연동 시트(영상+자막은 2회 호출 또는 자동 감지), 동일 파일명 자막 자동 탐색 결과 카드, "자막 없이 진행" 대체 경로, 형식/용량 안내 텍스트, 원형 진행 인디케이터
- **인터랙션**: 시스템 파일 선택기 호출(iOS: UIDocumentPicker / Android: SAF). 영상 선택 시 같은 디렉터리에서 동일 파일명(확장자만 다른) `.srt`/`.vtt`를 자동 탐색해 1탭으로 확인만 받고, 없으면 자막 파일을 직접 고르거나 "자막 없이 진행"으로 음성만 모드로 넘어갈 수 있음
- **상태 처리**: Empty(경로 선택 전 시트) / Loading(파일 불러오기 진행바) / Error(형식 미지원·용량 초과 시 인라인 에러 + "다른 파일 선택" 버튼, 자막 파일 파싱 실패 시 "자막 형식을 확인할 수 없어요 → 자막 없이 진행" 대체 제공) / Success(음성만 → 3번 "분리 중"으로 자동 전환 / 영상+자막 → 자막 파싱은 즉시 끝나 3번을 건너뛰고 바로 4번으로 전환) / Partial(해당 없음 — 로컬 처리라 중간에 끊겨도 재개는 단순 재시도로 충분)

---

### 3. 분리 중 (로컬 처리) — 음성만 업로드했을 때만 표시

**2026-08-05 변경**: 기존 "AI 분석 중" 화면을 완전히 대체한다. 서버로 파일을 보내 음성인식(STT)을 돌리는 것이 아니라, **기기 안에서 무음 구간(silence/pause)을 찾는 신호처리만** 수행한다. 영상+자막 경로는 자막 파일 파싱이 사실상 즉시 끝나므로 이 화면 자체가 나타나지 않고 2번에서 바로 4번으로 넘어간다. 이 화면은 "긴 음성 파일일 때만, 잠깐" 보이는 화면이며, 카피에서 네트워크 대기 뉘앙스(예: "N분 정도 걸려요")를 완전히 제거한다.

```
┌─────────────────────────┐
│                          │
│    ∿∿∿∿∿∿∿∿∿∿∿∿          │  ← 움직이는 파형 애니메이션
│                          │
│   문장 구간을              │
│   나누고 있어요            │
│                          │
│   ▓▓▓▓▓▓░░░░░░░░  42%    │
│                          │
│   🔒 이 기기에서만 처리돼요  │
│   서버로 전송되지 않아요     │
│                          │
│  ┌────────────────────┐ │
│  │   백그라운드에서 계속  │ │  ← 텍스트 버튼(닫아도 진행)
│  └────────────────────┘ │
└─────────────────────────┘
```
- **핵심 컴포넌트**: 파형 로딩 애니메이션(모션 감소 설정 시 정적 아이콘으로 대체), 실측 기반 진행률 %(무음 감지 알고리즘이 실제로 처리한 구간 비율 — 가짜 네트워크 타이머 아님), "이 기기에서만 처리됨" 안내 배지, 백그라운드 진행 허용 버튼
- **인터랙션**: 화면을 벗어나도 홈에 진행 중 카드로 표시되어 로컬 처리 계속됨(비차단형 대기). 별도의 개인정보 동의 절차는 없음 — 오디오가 기기 밖으로 나가지 않으므로 동의를 받을 대상 자체가 없음(기존 "STT 이용 동의" 바텀시트는 완전히 삭제).
- **상태 처리**: Loading(위 그림) / Error(로컬 처리 실패 시 — 예: 손상된 파일 — "다시 시도" + "일정 간격으로 균등 분할" 대체 경로 제공, 이 경우도 서버 재시도가 아닌 로컬 재계산) / Success(결과 화면 자동 전환) / Partial(해당 없음 — 로컬 처리는 부분 실패 개념이 없고 전부 처리되거나 재시도됨)

---

### 4. 문장 분리 결과 확인 — 자막모드 / 무자막모드 두 갈래

**2026-08-05 변경**: 더 이상 "AI가 나눴다"는 프레이밍을 쓰지 않는다. 두 모드는 화면 상단 안내 배너부터 다르게 시작한다.

**(A) 무자막모드 — 음성만 업로드, 무음 감지로 분리**
```
┌─────────────────────────┐
│ ← 24개 구간으로 나눴어요    │
│  ℹ️ 이 파일은 자막이 없어서  │
│    텍스트 없이 듣고 따라    │
│    말하기 모드로 진행돼요   │
│                          │
│  ══════▮▮▮▮══════▮▮══   │ ← 전체 파형 타임라인(스크롤/줌 가능)
│      ①    ②    ③        │   구간마다 경계 핸들( ▮ )
│                          │
│  문장 1                   │
│    00:03 – 00:07         │
│  ┌ ▮────────────▮ ┐     │ ← 개별 파형 미니뷰 + 좌우 드래그 핸들
│                          │
│  문장 2                   │
│    00:07 – 00:12         │
│                          │
│  문장 3                   │
│    00:12 – 00:14         │
│    [병합 ▲] [분리 ▼]     │
│                          │
│  … (스크롤)               │
│                          │
│  ┌────────────────────┐ │
│  │   24개 구간으로 시작   │ │  ← Primary CTA (하단 고정)
│  └────────────────────┘ │
└─────────────────────────┘
```

**(B) 자막모드 — 영상+자막 업로드, 자막 타임스탬프를 그대로 사용**
```
┌─────────────────────────┐
│ ← 자막 기준 18개 문장       │
│  ℹ️ 자막 파일의 타임스탬프를 │
│    그대로 사용해요. 자막이   │
│    기준이라 대부분 그대로    │
│    학습을 시작해도 좋아요    │
│                          │
│  ① "I think this is..." │
│    00:03 – 00:07         │
│                          │
│  ② "the best way to..." │
│    00:07 – 00:12         │
│                          │
│  ③ "understand it"       │
│    00:12 – 00:14         │
│                          │
│  … (스크롤)               │
│                          │
│  ┌────────────────────┐ │
│  │   18개 문장으로 시작   │ │  ← Primary CTA (하단 고정)
│  └────────────────────┘ │
└─────────────────────────┘
```
- **핵심 컴포넌트**: 상단 모드 안내 배너(무자막/자막 각각 다른 문구, 아이콘만이 아니라 텍스트로 명확히 설명), 전체 타임라인 파형(무자막모드만 해당, 줌/스크럽), 구간 카드 리스트(무자막모드: 순서번호+시간만 / 자막모드: 자막 텍스트+시간), 드래그 가능한 경계 핸들(무자막모드만), 병합/분리 버튼(무자막모드만 — 자막모드는 자막 자체가 기준이므로 경계 편집 UI 노출 안 함), 하단 고정 "학습 시작" CTA
- **인터랙션(상세는 "핵심 인터랙션 패턴" 참조)**: (무자막모드) 카드 탭→해당 구간 미리듣기, 경계 핸들 좌우 드래그로 미세조정(스냅 가이드), 두 카드를 위로 스와이프해 병합, 카드 롱프레스→분리 지점 지정 — 모두 "무음 감지가 살짝 어긋난 곳"을 사용자가 직접 고치는 로컬 편집이며 AI 신뢰도 개념은 없음. (자막모드) 카드 탭→미리듣기만 가능, 자막 타임스탬프와 실제 발화가 살짝 어긋나 있어도(자연스러운 자막 관례) 자막을 그대로 신뢰하고 자동 보정하지 않음 — 필요하면 사용자가 병합/분리를 선택적으로 쓸 수 있으나 기본 노출 우선순위는 낮춤
- **상태 처리**: Empty(해당없음, 항상 최소 1문장/1구간 보장) / Loading(카드 스켈레톤) / Error(무자막모드: 전체 무음 감지 실패 시 "일정 간격 균등 분할로 대체" 버튼 / 자막모드: 자막 파일 파싱 실패 시 "자막 없이 진행"으로 무자막모드 전환 제안) / Success(카드 리스트 정상 표시) / Partial(해당 없음 — 신뢰도 배지 등 부분 성공 개념 자체가 없어짐)

---

### 5. 쉐도잉 학습 화면 — 앱의 심장 (가장 상세히 설계)

**설계 원칙**: 화면에는 딱 4가지만 — ① 지금 문장, ② 반복 진행 상태, ③ 재생/녹음 트리거, ④ 최소한의 보조 컨트롤(시트로 숨김). 광고 없음. 알림 없음. 이 화면에 들어오면 오직 학습만 보이게 한다.

```
┌─────────────────────────┐
│ ✕        7 / 24      ⋯  │ ← 종료 / 진행위치 / 옵션(시트열기)
│  ▓▓▓▓▓▓▓░░░░░░░░░░░░░   │ ← 전체 문장 진행 바(얇게, 상단)
│                          │
│                          │
│   "I think this is       │
│    the best way to       │
│    understand it."       │  ← 문장 카드(가변 폰트, 최대 28sp)
│                          │
│   [ 한글 뜻 보기 ⌄ ]      │ ← 접힘 기본, 탭하면 아래 표시
│                          │
│      ∿∿∿∿●∿∿∿∿           │ ← 원어민 음성 파형(재생 위치 표시)
│                          │
│      ●  ●  ●  ○  ○      │ ← 반복 횟수 도트 (설정값 5회 중 3회완료)
│                          │
│                          │
│         ┌──────┐         │
│         │  🔊  │         │ ← 큰 원형 버튼: 상태에 따라
│         └──────┘         │    듣기 → 따라말하기(마이크) 전환
│       듣고 따라 말해보세요  │
│                          │
│  ⏮        ⏸/▶        ⏭  │ ← 보조 컨트롤(작게, 하단)
└─────────────────────────┘
```

**무자막모드 변형 (2026-08-05 추가) — 음성만 업로드한 파일은 텍스트 자체가 없음**
```
┌─────────────────────────┐
│ ✕        7 / 24      ⋯  │
│  ▓▓▓▓▓▓▓░░░░░░░░░░░░░   │
│                          │
│                          │
│        문장 7             │  ← 텍스트 대신 순번만 크게 표시
│    (자막 없음 · 듣고       │
│     따라 말하기 모드)      │  ← 작은 보조 라벨로 재확인
│                          │
│      ∿∿∿∿●∿∿∿∿           │ ← 원어민 음성 파형(재생 위치 표시)
│                          │
│      ●  ●  ●  ○  ○      │
│                          │
│         ┌──────┐         │
│         │  🔊  │         │
│         └──────┘         │
│       듣고 따라 말해보세요  │
│                          │
│  ⏮        ⏸/▶        ⏭  │
└─────────────────────────┘
```
이 모드에서는 "한글 뜻 보기" 토글 자체가 화면과 옵션 시트 양쪽에서 숨겨진다(번역할 텍스트가 없으므로). 대신 문장 카드 영역에 "자막 없음" 보조 라벨을 상시 노출해, 사용자가 학습 중간에 "왜 텍스트가 안 나오지"라고 혼란스러워하지 않도록 한다.

**핵심 루프 상태 전이 (한 문장 기준)**
```
[대기] → 탭/자동 → [원어민 음성 재생] (파형 진행)
   → 자동 → [따라 말하기] (마이크 아이콘 펄스, 3·2·1 카운트 후 녹음 표시)
   → 반복 도트 +1 채움
   → 설정 반복횟수 미도달 시 → [원어민 음성 재생]으로 자동 복귀
   → 반복횟수 도달 시 → 0.6초 완료 애니메이션(도트 전체 채움+체크)
   → Hands-free ON 이면 → 자동으로 다음 문장 [대기]
   → Hands-free OFF 이면 → 다음 문장으로 "밀어서 이동(스와이프↑)" 대기
```

**옵션 시트 (⋯ 탭 시, Bottom Sheet)**
```
┌─────────────────────────┐
│  ─                       │
│  학습 옵션                │
│  반복 횟수      3 – ● – 10│ ← 스텝퍼/슬라이더
│  재생 속도      0.75x ▾   │
│  한글 뜻 자동표시   ○────  │ ← 토글 (무자막모드에서는 항목 자체가 숨겨짐)
│  Hands-free 모드    ────● │ ← 토글(기본 ON)
│  내 목소리 녹음/비교  ○──── │ ← 토글(기본 OFF, 선택적 기능)
└─────────────────────────┘
```
- **핵심 컴포넌트**: 상단 진행 바, 문장 카드(자막모드: 가변 텍스트+Dynamic Type 대응 / 무자막모드: 순번 숫자+"자막 없음" 보조 라벨), 번역 접기/펼치기(자막모드에서만 노출), 원어민 파형 플레이어, 반복 도트 인디케이터, 원형 프라이머리 버튼(재생↔녹음 겸용, 상태별 아이콘/컬러 전환), 하단 보조 트랜스포트(이전/일시정지/다음), 학습 옵션 Bottom Sheet
- **인터랙션**: 원형 버튼 탭 1개로 듣기→말하기 전이(제스처 최소화가 핵심), 문장 카드를 위로 스와이프하면 즉시 다음 문장으로 스킵, 아래로 스와이프하면 이전 문장, 화면 어디든 롱프레스 시 0.5배속 임시 재생(프리뷰), 화면 항상 켜짐 유지(Wakelock)
- **상태 처리**: Loading(문장 오디오 버퍼링 시 원형 버튼에 스피너 오버레이) / Error(오디오 재생 실패 시 인라인 토스트 + 자동 재시도 1회) / Success(정상 루프) / Partial(마이크 권한 없을 시 "따라 말하기" 단계를 "다음" 자동 전환으로 대체하고 안내 배너 1회 노출) / Empty(해당없음)
- **비몰입 저해 요소 배제**: 이 화면에는 배너 광고·팝업·알림을 절대 노출하지 않음. 전면 광고는 세션 종료(#6) 화면 진입 시점에만 노출(스킵 가능 5초 후) — Hands-free 루프를 끊는 문장 단위 트리거는 2026-08-08 검토 후 폐기.

---

### 6. 학습 완료 요약

```
┌─────────────────────────┐
│                          │
│        🎉                │
│   오늘도 완료했어요!        │
│                          │
│   24문장 · 8분 12초        │
│   🔥 4일 연속 학습          │
│                          │
│  ┌──────┐ ┌──────┐      │
│  │이어서 │ │ 홈으로 │      │
│  │복습하기│ │       │      │
│  └──────┘ └──────┘      │
│                          │
│  [전면 광고 — 스킵 가능]    │ ← 선택적 노출 지점
└─────────────────────────┘
```
- **핵심 컴포넌트**: 완료 일러스트/컨페티 모션(모션감소 설정 시 정적), 세션 요약 수치, 스트릭 배지, 2개 CTA(복습/홈), 전면 광고 슬롯(스킵 가능, 5초 후)
- **전면 광고 트리거 규칙(2026-08-08 확정)**: 전면 광고는 **오직 이 세션 종료 화면 진입 시점 1회**에만 노출된다 — 학습 중 "N문장마다" 같은 구간 트리거는 검토 후 최종 폐기됐다(Hands-free 몰입 루프를 끊지 않기 위함). 마이 탭에서 **광고 제거(단발성 IAP, 14,900원, #9)**를 구매한 사용자에게는 이 슬롯 자체가 렌더링되지 않는다.
- **상태 처리**: Success(요약 표시) / Partial(마지막 문장까지 못 갔을 시 "이어하기" 문구로 대체) — Empty/Loading/Error 해당없음(즉시 계산 가능한 로컬 데이터)

---

### 7. ~~라이브러리 홈~~ — 2026-08-08 삭제됨

**삭제됨(2026-08-08)**: CNN/BBC 공식 자막 기반 큐레이션 콘텐츠 목록 화면. 라이브러리 탭 전체가 별도 앱 "English News Shadowing Lab"으로 이전되며 이 앱의 코드에서 완전히 삭제됐다(`AiAnalyzingScreen` 이후 라우팅 테이블 어디에도 라이브러리 관련 라우트가 남아있지 않음, `app_router.dart` 확인 완료). 아래 이전 설계 내용은 삭제 이력 참고용으로만 남기며, 이 앱의 현재 화면 구성이 아니다.

<details>
<summary>삭제 전 원본 설계 (참고용, 이 앱에는 더 이상 존재하지 않음)</summary>

**2026-08-05 변경**: "오늘의 뉴스"는 AI 음성인식이 아니라 **CNN/BBC가 공개하는 공식 자막/트랜스크립트**를 매일 가져와 등록한 콘텐츠다(개발자가 매일 수작업/파이프라인으로 등록하는 고정 비용이며, 사용자당 과금이 아님). 콘텐츠 카드와 상세 화면 모두에 출처를 시각적으로 명확히 표시해 "이건 진짜 방송사 자막"이라는 신뢰를 준다.

```
┌─────────────────────────┐
│  라이브러리          👑PRO│
│                          │
│  오늘의 뉴스 (매일 06시 갱신)│
│  공식 자막 기반 · AI 아님   │  ← 서브카피로 출처 신뢰 보강
│  ┌────────────────────┐ │
│  │ 🖼 "AI reshapes..."  │ │
│  │  [CNN] · 3분 · 중급   │ │  ← 출처 로고/배지(작은 사각 라벨)
│  └────────────────────┘ │
│  ┌────────────────────┐ │
│  │ 🖼 "Markets rally.." │ │
│  │  [BBC] · 4분 · 중상급 │ │
│  └────────────────────┘ │
│                          │
│  주제별                   │
│  [일상회화][비즈니스][여행]│  ← 가로 스크롤 칩
│                          │
│  회화                     │
│  ┌───┐ ┌───┐ ┌───┐      │
│  │카페│ │공항│ │면접│      │ ← 카드형 가로 스크롤
│  └───┘ └───┘ └───┘      │
│                          │
│ (비구독자: 카드 하단에      │
│  블러+🔒 오버레이, 상단에   │
│  "무료로 3개 체험하기" 배너)│
└─────────────────────────┘
```
- **핵심 컴포넌트**: PRO 상태 배지(우상단), 카테고리 섹션(오늘의 뉴스/주제별/회화), 콘텐츠 카드(썸네일+출처 배지([CNN]/[BBC] 로고 또는 텍스트 라벨)+길이+난이도 칩), 비구독자용 잠금 오버레이 + 체험 배너
- **인터랙션**: 카드 탭 → 상세(구 #8)로 이동, 비구독 시 잠긴 카드도 탭 가능(미리듣기 유도, 즉시 paywall 강제 X)
- **상태 처리**: Empty(콘텐츠 서버 오류 시 "오늘의 콘텐츠를 불러올 수 없어요" + 캐시된 어제 콘텐츠 표시) / Loading(카드 스켈레톤) / Error(재시도 버튼) / Success(정상) / Partial(일부 카테고리만 로드 실패 시 해당 섹션만 스켈레톤 유지)

</details>

---

### 8. ~~콘텐츠 상세 / 잠금(Paywall) 시트~~ — 2026-08-08 삭제됨

**삭제됨(2026-08-08)**: 위 #7과 함께 별도 앱으로 이전됐다. 이 화면이 담당하던 "잠긴 콘텐츠 안내 + 결제 유도" 역할은 이 앱에서는 더 이상 존재하지 않는다 — 학습 기능 자체가 잠겨있지 않기 때문이다(광고 유무만 유료 전환 대상, 아래 #9 참고).

<details>
<summary>삭제 전 원본 설계 (참고용, 이 앱에는 더 이상 존재하지 않음)</summary>

```
┌─────────────────────────┐
│ ←  🖼 대표 이미지          │
│  "AI reshapes the        │
│   future of work"        │
│  [CNN] · 3:20 · 중급 · 문장18│  ← 출처 배지 + 메타 칩
│  ⓘ CNN 공식 자막 기반 콘텐츠│  ← 출처 신뢰 카피(AI 생성 아님 명시)
│                          │
│  ┌────────────────────┐ │
│  │  ▶ 30초 미리 듣기     │ │  ← 비구독자도 항상 가능
│  └────────────────────┘ │
│                          │
│  ┌────────────────────┐ │
│  │   PRO로 전체 학습하기 │ │  ← 비구독자: Paywall 시트 오픈
│  └────────────────────┘ │  ← 구독자: 바로 학습화면 진입
└─────────────────────────┘

Paywall 시트 (Bottom Sheet, 잠금 콘텐츠 접근 시):
┌─────────────────────────┐
│  ─                       │
│  👑 쉐도잉랩 프리미엄       │
│  매일 갱신되는 뉴스·회화로   │
│  끊임없이 쉐도잉하세요       │
│  ✓ 매일 새 콘텐츠           │
│  ✓ 광고 없이 학습           │
│  ✓ 무제한 반복/속도조절      │
│                          │
│  ○ 월 4,900원              │
│  ● 연 39,000원 (33%↓) 인기 │
│                          │
│  ┌────────────────────┐ │
│  │   시작하기 (7일 무료) │ │
│  └────────────────────┘ │
│  구독 취소는 언제든 가능      │
└─────────────────────────┘
```
- **핵심 컴포넌트**: 콘텐츠 히어로 이미지, 출처 배지([CNN]/[BBC] 로고 또는 텍스트 라벨) + 메타 정보 칩(길이/난이도/문장수), 출처 신뢰 카피, 미리듣기 버튼, PRO CTA, Paywall Bottom Sheet(가치 리스트, 요금제 라디오 카드, 무료체험 CTA, 취소 안내)
- **상태 처리**: Loading(미리듣기 버퍼링) / Error(결제 실패 시 인라인 에러+재시도) / Success(구독 완료 시 시트 닫히고 학습화면 자동 진입) / Empty·Partial 해당없음

</details>

---

### 9. 광고 제거 구매 시트 — 단발성 IAP (2026-08-08 신규, 옛 구독 결제/Paywall 대체)

**설계 배경**: 정기구독+콘텐츠 잠금 기반 Paywall(구 #9)을 대체한다. 이 앱에는 더 이상 잠글 콘텐츠가 없으므로(학습 기능 전부 무료), 유료 전환의 유일한 이유는 "광고를 없애고 싶다"뿐이다. 그래서 플랜 선택(월간/연간) 없이 **단일 SKU, 단발성 결제 하나**로 최대한 단순화했다. 코드 기준(`ad_removal_sheet.dart`): 구매 완료 시 시트가 자동으로 닫히고, 실패 시 인라인 에러 토스트, 복원은 별도 텍스트 링크로 처리한다.

```
마이 홈 → "🚫 광고 제거하기 · 14,900원" 배너 탭 → Bottom Sheet 오픈:

┌─────────────────────────┐
│  ─                       │
│  광고 제거                │
│  14,900원                 │
│                          │
│  ✓ 배너 광고 제거          │
│  ✓ 전면 광고 제거          │
│                          │
│  ┌────────────────────┐ │
│  │    광고 제거하기      │ │  ← Primary CTA (구매, 로딩 시 스피너)
│  └────────────────────┘ │
│                          │
│     이미 구매하셨나요?      │
│       복원하기            │  ← 텍스트 버튼 링크 (복원 중 인라인 스피너)
└─────────────────────────┘

구매 완료 후 — 마이 홈 카드 상태 전환:
┌─────────────────────────┐
│  ✓ 광고 제거됨             │  ← 배너/CTA 카드가 완료 배지로 교체
└─────────────────────────┘
```
- **핵심 컴포넌트**: 드래그 핸들, 타이틀+가격(고정 SKU 1개, 요금제 선택 UI 없음), 혜택 불릿 2개(배너 제거/전면 제거 — `Icons.check_circle` + 텍스트, 색상 단독 의존 아님), Primary 구매 버튼(`PrimaryButton`, 로딩 상태 지원), 복원 텍스트 링크(로딩 스피너 포함)
- **인터랙션**: 마이 홈의 "광고 제거하기" 배너 카드(미구매 시에만 노출) 탭 → `showModalBottomSheet`로 오픈. 구매 성공 시 시트가 즉시 닫히고 마이 홈 카드가 "광고 제거됨 ✓"으로 전환되며, 이후 홈 탭 배너와 세션 종료(#6) 전면 광고가 전부 사라진다. 복원 성공 시에도 동일하게 시트가 닫히고 같은 상태로 전환되며, 복원할 구매 내역이 없으면 정보성 토스트만 띄우고 시트는 유지된다.
- **상태 처리**:
  - **Empty**: 해당없음(항상 고정된 단일 SKU 카드 1개만 표시, 목록 개념 없음)
  - **Loading**: 구매 버튼 내부 스피너(구매 처리 중 버튼 비활성) / 복원 링크 내부 16dp 스피너(복원 처리 중)
  - **Error**: 구매/복원 실패 시(`PurchaseFailure`) 인라인 에러 토스트로 실패 사유 안내, 시트는 닫히지 않고 재시도 가능 / 알 수 없는 예외는 공통 실패 문구("결제 처리에 실패했어요. 다시 시도해주세요.")로 대체
  - **Success**: 구매 또는 복원 성공 시 시트 자동 닫힘 + 마이 홈 카드가 "광고 제거됨 ✓"으로 즉시 반영(로컬 구매 상태 스트림 구독)
  - **Partial**: 복원 시도했지만 구매 내역이 없는 경우("복원할 구매 내역이 없어요" 정보 토스트) — 실패는 아니므로 Error와 구분

---

### 10. 마이 홈

```
┌─────────────────────────┐
│  마이                     │
│                          │
│  ┌────────────────────┐ │
│  │ 🔥 4일 연속 학습중     │ │
│  │ 이번 주: ▓▓▓▓░░░      │ │ ← 요일별 미니 히트맵
│  │ 총 128문장 · 42분      │ │
│  └────────────────────┘ │
│                          │
│  ┌────────────────────┐ │
│  │ 🚫 광고 제거하기       │ │ ← 광고 제거 구매 유도 카드
│  │ 14,900원          >  │ │    (미구매 시에만 노출, 탭 시 #9 시트 오픈)
│  └────────────────────┘ │
│  (구매 완료 시 위 카드 대신) │
│  ┌────────────────────┐ │
│  │ ✓ 광고 제거됨          │ │
│  └────────────────────┘ │
│                          │
│  전체 학습 기록  >         │
│  ──────────────         │
│  학습 기본값     >         │
│  알림 설정       >         │
│  화면 (라이트/다크) >       │
│  계정            >         │
│  고객지원        >         │
│  ──────────────         │
│  버전 1.0.0               │
├─────────────────┬─────────────────┤
│      🏠 홈      │      👤 마이      │
└─────────────────┴─────────────────┘
```
- **핵심 컴포넌트**: 스트릭 요약 카드(히트맵 미니어처), 광고 제거 구매 상태 카드(미구매: CTA 배너 / 구매완료: 완료 배지), 리스트형 메뉴(학습기록/설정 항목들)
- **상태 처리**: Empty(학습기록 없을 시 "첫 학습을 시작해보세요" 카드) / Loading(카드 스켈레톤) / Success(정상) / Error·Partial 해당없음(로컬 통계)

---

### 11. 학습 기록 상세

```
┌─────────────────────────┐
│ ← 학습 기록                │
│                          │
│   8월                     │
│  일 월 화 수 목 금 토       │
│  ▢ ▢ ▢ ▢ ▨ ▨ ▩          │ ← 학습량별 색농도(GitHub 잔디 스타일)
│  ...                     │
│                          │
│  누적 통계                 │
│  총 학습일수     18일       │
│  총 문장수      412개       │
│  총 학습시간    2시간 41분   │
└─────────────────────────┘
```
- **핵심 컴포넌트**: 캘린더 히트맵(색상만이 아닌 진하기+숫자 병기로 접근성 확보), 누적 통계 리스트
- **상태 처리**: Empty("아직 기록이 없어요") / Success(정상) / Loading(스켈레톤)

---

### 12. 설정

```
┌─────────────────────────┐
│ ← 설정                    │
│  학습 기본값                │
│    기본 반복 횟수    5회 > │
│    기본 재생 속도  1.0x > │
│    Hands-free 모드   ●── │
│  알림                     │
│    학습 리마인더      ──● │
│    알림 시간        20:00>│
│  화면                     │
│    테마       시스템 설정 >│
│    글자 크기        보통 >│
│  계정                     │
│    로그인 계정   ***@gmail>│
│    데이터 백업/복원      > │
│  지원                     │
│    자주 묻는 질문        > │
│    문의하기              > │
│    이용약관/개인정보처리방침>│
│  버전 정보          1.0.0 │
└─────────────────────────┘
```
- **핵심 컴포넌트**: 그룹핑된 리스트(섹션 헤더+아이템), 토글 스위치, 화살표(>)로 상세 이동 표시
- **상태 처리**: Success(정상 표시) / Error(백업 실패 시 토스트) — Empty/Loading/Partial 해당없음

---

## 핵심 인터랙션 패턴

### 1) 쉐도잉 반복 학습 루프 (앱의 심장)

- **원버튼 원칙**: 화면 중앙 원형 버튼 하나가 상태에 따라 "듣기(스피커 아이콘)" ↔ "따라 말하기(마이크 아이콘)"로 형태와 색이 전환된다. 사용자는 다른 컨트롤을 찾을 필요 없이 이 버튼만 반복해서 인지하면 된다.
- **자동 진행(Hands-free, 기본 ON)**: 원어민 음성 재생 → 0.3초 대기 → 마이크 활성(사용자 말하는 시간은 문장 길이의 약 1.3배로 자동 산정, 설정에서 배수 조정 가능) → 반복 도트 채움 → 설정 횟수만큼 자동 반복 → 문장 간 자동 전환. 사용자는 화면만 보고 있어도 학습이 진행된다.
- **수동 개입은 항상 가능**: 스와이프 업(다음 문장) / 다운(이전 문장), 원형 버튼 탭으로 즉시 재생 재시작, 롱프레스로 0.5배속 프리뷰.
- **피드백**: 반복 완료마다 도트가 채워지며 미세 햅틱(Light Impact) 1회, 문장 완료 시 도트 전체가 체크 아이콘으로 전환 + 햅틱(Medium) — 시각+촉각 이중 피드백으로 몰입 유지.
- **광고 배제 구간**: 이 루프 진행 중에는 배너/전면 광고를 절대 삽입하지 않는다. 전면 광고는 세션 종료 후 #6 요약 화면 진입 시점에서만 노출.

### 2) 문장 경계 편집 제스처 (참고 앱 대비 최대 차별화 지점, 2026-08-05 갱신)

참고 앱은 사용자가 SRT 타임스탬프를 직접 입력/편집해야 하는 반면, 쉐도잉랩은 **로컬 자동 분리 결과를 보고 필요한 곳만 미세조정** 하는 방식으로 진입장벽을 없앤다. 단, 이 편집 제스처들은 **무자막모드(음성만 업로드, 무음 감지)에만 적용**된다 — 자막모드(영상+자막)는 자막 타임스탬프가 기준이므로 경계 편집 UI 자체를 노출하지 않는다(자막이 실제 발화와 살짝 어긋나 있어도 자막을 그대로 신뢰하고 자동 보정하지 않음, 필요 시 병합만 선택적으로 제공).

| 제스처 | 동작 | 시각 피드백 |
|---|---|---|
| 경계 핸들(▮) 좌우 드래그 | 문장 시작/끝 시간 미세조정 | 드래그 중 파형 확대 표시 + 무음구간에 자동 스냅 가이드라인(자석 효과) |
| 카드 탭 | 해당 구간만 미리듣기 재생 | 카드 하이라이트 + 미니 파형 재생 위치 이동 |
| 두 카드를 서로 위로 스와이프(병합 제스처) | 인접 구간 병합 | 두 카드가 하나로 합쳐지는 모핑 애니메이션 |
| 구간 롱프레스 → 분리 지점 탭 | 한 구간을 두 구간으로 분리 | 분리선 프리뷰 후 확정 시 카드 2개로 분할 애니메이션 |
| 전체 되돌리기 | 상단 "자동 분리 결과로 초기화" 텍스트 버튼 | 모든 수동 편집 취소 확인 다이얼로그 |

이 편집 화면은 **"안 고쳐도 되는 화면"**이 기본값이다 — 무음 감지/자막 매칭 결과가 충분히 정확하면 사용자는 그냥 하단 CTA만 누르고 지나가면 된다. 편집 기능은 필요할 때만 존재를 드러낸다(Progressive Disclosure). 신뢰도(confidence) 배지는 더 이상 존재하지 않는다 — STT를 사용하지 않으므로 "이 구간은 AI가 얼마나 확신하는지"를 나타낼 근거 자체가 없다.

### 3) 무료 ↔ 유료(광고 제거) 전환 인터랙션 (2026-08-08 갱신)

**옛 "무료 ↔ 프리미엄 전환" 절 전면 대체** — 콘텐츠 잠금 기반 Paywall 개념 자체가 사라졌으므로(라이브러리 탭 분리로 잠글 콘텐츠가 없음) 아래 원칙으로 갱신한다.

- 이 앱의 모든 학습 기능(파일 업로드, 문장 분리, 쉐도잉 학습, 반복/속도 조절 등)은 처음부터 끝까지 완전히 무료다 — 기능 단위 잠금이나 유료 게이트가 존재하지 않는다.
- 유료 전환의 유일한 동기는 "광고를 안 보고 싶다"이며, 강제 노출이 아닌 **마이 탭의 선택적 CTA 카드**(#10)로만 유도한다 — 별도의 강제 팝업/전면 유도는 없다.
- 광고 제거 구매 시트(#9)는 전체화면 대신 Bottom Sheet로 노출해 "언제든 닫고 돌아갈 수 있는" 압박감 낮은 결제 유도를 한다. 요금제 선택 라디오도, 무료체험/구독취소 안내도 없다 — 가격 하나, 버튼 하나, 복원 링크 하나뿐이다.
- 구매(또는 구매 복원) 완료는 로컬 구매 상태 스트림으로 즉시 반영되어 홈 탭 배너와 세션 종료(#6) 전면 광고가 앱 재시작 없이 바로 사라진다.

### 4) 모션/트랜지션 스펙

| 상황 | 트랜지션 | duration | easing |
|---|---|---|---|
| 탭 전환 | Fade + 없음(즉시) | 0ms | - |
| 스택 Push/Pop | 우→좌 슬라이드(iOS 스타일 통일) | 280ms | easeOutCubic |
| Bottom Sheet 오픈 | 하단에서 슬라이드업 + 배경 딤 | 220ms | easeOut |
| 문장 카드 전환(학습화면) | 수직 스와이프 + 페이드 | 250ms | easeInOutCubic |
| 반복 도트 채움 | 스케일 팝(1→1.15→1) | 150ms | spring |
| 병합/분리 애니메이션 | 모핑(높이/위치 보간) | 300ms | easeInOutQuart |
| 모션 감소 설정 시 | 위 모든 트랜지션을 Fade 120ms로 대체 | - | - |

---

## 디자인 시스템

### 컬러 팔레트

| 용도 | Light Mode | Dark Mode |
|---|---|---|
| Primary | `#5B5FEF` (Indigo 500) | `#8B8FFF` (Indigo 300) |
| On Primary | `#FFFFFF` | `#14141A` |
| Primary Container | `#E4E3FF` | `#2C2B57` |
| Secondary/Accent (파형·성공) | `#14B8A6` (Teal 500) | `#2DD4BF` (Teal 400) |
| Background | `#FAFAFC` | `#0E0E12` |
| Surface (카드/시트) | `#FFFFFF` | `#1A1A20` |
| Surface Variant | `#F2F2F7` | `#232329` |
| Text Primary | `#17171C` | `#F2F2F5` |
| Text Secondary | `#6B6B76` | `#A0A0AC` |
| Outline/Divider | `#E4E4EA` | `#303038` |
| Error | `#E5484D` | `#FF6369` |
| Success | `#22C55E` | `#4ADE80` |

- 모든 텍스트-배경 조합은 WCAG 2.1 AA(4.5:1) 이상 확보 (Primary `#5B5FEF` on White = 4.6:1, 버튼 텍스트는 White on Primary로 고정).
- 다크모드는 시스템 설정 자동 감지 + 설정에서 수동 강제 가능(라이트/다크/시스템 3옵션).
- **2026-08-08**: "PRO 포인트(Gold)" 토큰은 삭제했다 — PRO 배지/잠금 UI가 이 앱에서 완전히 제거되며 쓰임이 없어졌고, 코드(`app_colors.dart`/`app_theme.dart`)에서도 `proGold`가 이미 삭제되어 있음을 확인했다.

### 타이포그래피

- **서체**: Pretendard Variable (한글 UI 기본, iOS/Android 공통 적용으로 플랫폼 간 일관성 확보) + 숫자/영문은 시스템 폰트(SF Pro/Roboto) 자동 폴백
- 문장 학습 카드의 **영어 원문**은 가독성을 위해 자간을 넓힌 Pretendard 사용, 최소 18sp ~ 사용자 설정(Dynamic Type) 최대 200%까지 확대 허용

| 스타일 | 서체 | 크기 | 무게 | 용도 |
|---|---|---|---|---|
| Display | Pretendard | 32sp | Bold | 온보딩 헤드라인 |
| Headline | Pretendard | 24sp | SemiBold | 화면 타이틀 |
| Title | Pretendard | 18sp | Medium | 카드 타이틀, 섹션 헤더 |
| **쉐도잉 문장 텍스트** | Pretendard | 22~28sp (가변) | Medium | 학습화면 핵심 텍스트, Dynamic Type 대응 |
| Body | Pretendard | 15sp | Regular | 본문, 리스트 서브텍스트 |
| Label | Pretendard | 13sp | Medium | 버튼, 칩, 배지 |
| Caption | Pretendard | 12sp | Regular | 타임스탬프, 메타정보 |

### 스페이싱/그리드 (8dp 기반)

| 토큰 | 값 | 용도 |
|---|---|---|
| xs | 4dp | 아이콘-텍스트 간격 |
| sm | 8dp | 카드 내부 패딩 |
| md | 16dp | 화면 좌우 마진(기본), 요소 간 간격 |
| lg | 24dp | 섹션 간 구분 |
| xl | 32dp | 주요 섹션 간 |
| 2xl | 48dp | 온보딩/히어로 영역 |
| 화면 마진 | 20dp | 전 화면 좌우 공통 마진 |
| 카드 radius | 16dp | 카드/시트 |
| 버튼 radius | 12dp (Full-width) / 999dp (원형 CTA) | |
| 최소 터치 타깃 | 48dp (Android) / 44pt (iOS) | 모든 인터랙션 요소 공통 |

### 아이콘 & 모션

- **아이콘 세트**: 라인 스타일 통일(Phosphor Icons 또는 Material Symbols Outlined), 24dp 표준/20dp 소형, 스트로크 1.5px 통일 — iOS/Android 어디서든 동일 세트 사용해 플랫폼 간 이질감 제거
- **모션 원칙**: 모든 상태 변화에 최소 1개의 모션 피드백(위 트랜지션 표 참조), `prefers-reduced-motion`/OS 모션감소 설정 감지 시 전부 Fade 120ms로 대체

### 컴포넌트 목록

| 컴포넌트 | 변형(Variants) | 상태 |
|---|---|---|
| Button | Primary(Filled) / Secondary(Tonal) / Text / Icon-only / 원형 CTA | Default / Pressed / Disabled / Loading |
| Sentence Card | 목록형(편집화면) / 학습형(학습화면), 자막모드(텍스트 있음) / 무자막모드(순번만) | Idle / Playing / Recording / Completed |
| Repeat Dot Indicator | 3~10개 가변 | Empty / Filled / Current(pulse 애니메이션) |
| Boundary Handle | Left / Right | Default / Dragging / Snapped |
| Waveform Player | Compact(카드 내) / Expanded(학습화면) | Idle / Playing / Buffering / Dragging |
| Bottom Sheet | Half-height / Full-height | Expanding / Collapsed |
| Tab Bar Item | 홈/마이 (2026-08-08: 라이브러리 제거) | Active / Inactive / Badge(NEW 알림점) |
| Toast/Snackbar | Success / Error / Info | Enter / Exit(자동 3초) |
| Skeleton Loader | List-row / Card / Circle | Loading |
| Empty State | 일러스트+타이틀+설명+CTA | - |
| Ad Removal Card (마이 홈, 2026-08-08 신규) | CTA(미구매) / Purchased(완료 배지) | Default / Pressed |
| Progress Bar | Linear(파일카드) / Circular(업로드) | 0~100% |
| Calendar Heatmap Cell | 학습량 4단계 농도 | Empty(0) / Low / Mid / High |

- **2026-08-08 삭제된 컴포넌트**: `PRO Badge/Chip`, `Source Attribution Badge`(CNN/BBC), `Paywall Card`(Monthly/Yearly) — 라이브러리 탭/구독 기능과 함께 완전히 제거됐다. 이 컴포넌트들은 English News Shadowing Lab 앱 쪽 문서에서 계속 관리된다.

---

## 접근성(A11y) 고려사항

- **터치 타깃**: 전 컴포넌트 최소 48×48dp(Android)/44×44pt(iOS) 준수. 특히 학습화면 원형 CTA는 72dp로 확대(핵심 액션이므로 오터치 방지 + 접근성 동시 확보).
- **색상 대비**: 전 텍스트-배경 조합 WCAG AA(4.5:1) 이상, 대형 텍스트(학습 문장)는 3:1 이상. Dark/Light 모드 각각 별도 검증.
- **색상 단독 의존 금지**: 무자막모드의 "텍스트 없음" 상태는 아이콘만이 아니라 "자막 없음" 텍스트 라벨을 항상 병기. 광고 제거 구매 상태(#9/#10)도 체크 아이콘(`Icons.check_circle`)과 "광고 제거됨" 텍스트를 함께 병기. 캘린더 히트맵도 색 농도+숫자 병기. (2026-08-08: "PRO 잠금은 색상(Gold)뿐 아니라 🔒 아이콘+PRO 텍스트 병기", "콘텐츠 출처(CNN/BBC) 로고 색상뿐 아니라 텍스트 라벨 병기" 항목은 해당 기능이 삭제되며 함께 제거)
- **스크린리더(VoiceOver/TalkBack)**:
  - 학습화면 원형 CTA는 상태별로 동적 accessibilityLabel 제공: "재생, 원어민 음성 듣기" ↔ "녹음 중, 따라 말하는 중"
  - 반복 도트는 "5회 중 3회 완료"로 텍스트 대체 안내(개별 도트 순회 대신 요약 발화)
  - 파형 시각 요소는 장식용으로 처리(`excludeSemantics`)하고, 재생 위치는 별도 진행률 텍스트로 제공
  - 경계 핸들 드래그는 스크린리더 사용자를 위해 대체 입력 경로 제공(핸들 선택 후 "+0.1초/-0.1초" 스테퍼 버튼으로도 조정 가능)
  - 무자막모드 문장 카드는 "문장 7, 자막 없음, 듣고 따라 말하기 모드"로 발화해 텍스트 부재가 오류가 아님을 명확히 전달
- **Dynamic Type / 글자 크기**: 학습 문장 텍스트는 최대 200%까지 확대 시에도 레이아웃 깨짐 없이 스크롤 처리(고정 높이 카드 지양). 설정에도 별도 "글자 크기" 옵션 제공(OS 설정 미반영 기기 대비).
- **모션 축소**: OS `Reduce Motion` 설정 감지 시 파형 애니메이션, 카드 전환, 컨페티 등 모든 비필수 모션을 정적/즉시 전환으로 대체.
- **청각 대체**: 마이크 권한 미허용 사용자를 위해 "따라 말하기" 단계를 건너뛰고 듣기 반복만으로도 학습 루프가 정상 동작하도록 설계(음성 인식/녹음은 필수 아님, 선택 기능).
- **키보드/외부 입력**: 태블릿/폴더블 대응 시 외장 키보드로 Tab 이동·Space 재생/일시정지·화살표 이전/다음 문장 지원.
- **포커스 순서**: 모든 화면에서 상단→본문→하단 CTA 순으로 논리적 포커스 순서 보장, 모달/시트 오픈 시 포커스 트랩 적용.

---

## 앱 개발자 전달 사항

- **아키텍처 권장**: Flutter, 상태관리는 화면 간 재사용이 많은 학습 루프(#5)를 고려해 Riverpod/Bloc 등 예측 가능한 상태관리 채택 권장. (2026-08-08: 라이브러리 탭이 별도 앱으로 분리되며 "Tab1/Tab2 데이터 소스만 주입" 요구사항은 소멸 — 쉐도잉 학습 화면은 이제 내 파일(로컬) 소스 하나만 다루면 된다.)
- **공통 위젯화 우선순위**: `SentenceCard`(자막모드/무자막모드 두 변형), `RepeatDotIndicator`, `WaveformPlayer`, `BoundaryHandle`, `AdRemovalSheet`(단발성 IAP, `PaywallSheet` 대체), `PrimaryButton`(CTA), `TabScaffold`(공통 배너광고 슬롯 포함, 2탭) — 이 7개를 디자인 시스템 컴포넌트 라이브러리로 최우선 구현 요청. (`PaywallSheet`/`SourceAttributionBadge`는 2026-08-08 삭제 대상에서 이미 구현 제거 완료 — `ad_removal_sheet.dart`로 대체됨.)
- **오디오/비디오 재생**: `just_audio` + `just_audio_background`(백그라운드 재생), 영상 파일은 오디오만 추출해 재생해도 무방(화면엔 파형만 노출하므로 비디오 렌더링 불필요 — 성능/배터리 이점).
- **문장 분리 파이프라인 (2026-08-05 확정 — 서버/외부 API 완전 배제)**:
  - **음성만 업로드**: 기기 내에서 오디오 진폭(RMS/dB) 분석으로 무음 구간을 찾아 문장 경계를 추정하는 로컬 신호처리 알고리즘 구현 필요(예: 임계 dB 이하가 일정 시간(예: 400ms) 이상 지속되면 경계로 판단). 결과는 텍스트 없이 `{index, startMs, endMs}`만 생성.
  - **영상+자막 업로드**: 클라이언트에서 SRT/VTT 파일을 파싱해 각 cue의 타임스탬프를 문장 경계로, cue 텍스트를 문장 텍스트로 그대로 사용(서버 파싱/보정 없음). 자막 인코딩(UTF-8 BOM 포함) 예외 처리 필요.
  - 두 경로 모두 네트워크 호출이 전혀 없으므로, 업로드 후 처리 실패는 전부 "로컬 처리 실패"로 다루고 재시도는 로컬 재계산으로 처리.
  - 기존에 계획했던 STT 이용 동의 바텀시트, "AI 분석 중" 진행 화면, STT 신뢰도(confidence) 배지는 전부 삭제 대상 — 코드베이스에 남아있다면 함께 제거할 것.
- **로컬 저장**: 업로드 파일·분리 결과(문장/구간 타임스탬프, 자막모드 여부)는 기기 로컬(SQLite/Drift 또는 Isar) 저장, 클라우드 동기화는 선택 기능으로 후순위.
- **Wakelock**: 학습화면(#5) 진입 시 화면 꺼짐 방지(`wakelock_plus`) 적용 필수 — Hands-free 모드의 핵심 전제조건.
- **다크모드**: 시스템 감지 기본 + 앱 내 수동 오버라이드, `ThemeMode` 3단 지원.
- **광고 SDK 배치**: 배너는 `TabScaffold` 레벨에서 하단 고정 슬롯으로 공통 관리하되 **홈 탭(인덱스 0)에서만** 렌더링(마이 탭에는 배너 없음), 전면 광고는 세션 종료(#6) 화면 진입 시점에만 트리거하는 이벤트 훅 필요 — 문장 단위/구간 트리거는 없음. 학습화면(#5) 내부에는 광고 SDK 초기화 호출 자체를 넣지 않을 것(실수로 인한 몰입 저해 방지). **광고 제거 구매(#9) 완료 사용자는 두 슬롯 모두 인덱스/화면과 무관하게 렌더링하지 않을 것** — 구매 상태 스트림(`watchAdsRemoved()`)을 배너/전면 광고 트리거 지점 모두에서 구독해야 함.
- **광고 제거 IAP (2026-08-08 신규)**: 단일 SKU("remove_ads" 성격, 스토어별 상품 ID는 App Store Connect/Play Console에서 등록), 가격 14,900원. `purchaseRepositoryProvider`에 `purchaseRemoveAds()` / `restorePurchases()` / `watchAdsRemoved()` 세 메서드로 이미 구현되어 있으므로(코드 기준 `ad_removal_sheet.dart`), 신규 화면은 이 계약을 그대로 따를 것 — 구매/복원 실패는 `PurchaseFailure`로 구분해 사용자에게 구체적 실패 사유를, 알 수 없는 예외는 공통 실패 문구를 노출한다.
- **에러 바운더리**: 로컬 분리 실패(#3 Error, 무음 감지/자막 파싱 실패)와 광고 제거 구매/복원 실패(#9 Error)는 반드시 "재시도" 경로를 UI에 노출하도록 각 화면 상태 처리 명세(위 화면별 섹션) 그대로 구현.

## API 연동 전달 사항

**2026-08-08 갱신 — 라이브러리 API 전면 삭제**: 아래 `/library/*` 엔드포인트와 관련 데이터 필드는 라이브러리 탭이 별도 앱으로 분리되며 이 앱에는 더 이상 필요하지 않다(English News Shadowing Lab 쪽 API 연동 문서에서 계속 관리). 이 앱에 남는 서버 연동은 스토어 인앱결제 영수증 검증과 통계 동기화뿐이며, 둘 다 선택적(로컬 우선)이다.

- **필요 엔드포인트(추정) — 문장 분리는 서버 API가 필요 없음(로컬 처리)**:
  1. ~~`GET /library/today`, `GET /library/categories`, `GET /library/content/{id}`~~ — **삭제(2026-08-08)**. 프리미엄 큐레이션 콘텐츠 API는 라이브러리 탭과 함께 이 앱에서 완전히 제거됐다.
  2. `POST /purchase/verify` (또는 스토어 SDK 자체 검증) — 광고 제거 단발성 IAP 영수증 검증(App Store/Play Billing). 옛 `POST /subscription/verify`(구독 검증)를 대체 — 검증 대상이 "구독 갱신 여부"에서 "단일 상품 구매 여부"로 단순화됐다.
  3. `GET /user/stats` — 스트릭/누적 학습 통계(로컬 우선 계산 + 서버 동기화는 로그인 사용자만)
  4. (선택) 로그인 사용자의 내 파일 분리 결과 클라우드 백업/복원 엔드포인트 — v2 검토
- **화면별 필요 데이터 필드**:
  - #4 문장 분리 결과(내 파일, 전량 로컬 생성 — 서버 응답 아님): `segmentId, index, startMs, endMs, text(nullable — 자막모드만 존재, 무자막모드는 null), hasSubtitle(bool), edited(bool)` — **confidence 필드 없음**(STT 미사용이므로 신뢰도 개념 자체가 없음)
  - #5 학습화면: 위 필드 + `audioUrl 또는 localPath, translation(optional, hasSubtitle이 true일 때만 유효), repeatCountDefault`
  - #9 광고 제거 구매 시트: `productId(단일 SKU), priceLabel("14,900원"), isPurchased(bool, watchAdsRemoved() 스트림 기반)` — 서버 응답이 아니라 스토어 클라이언트 SDK 상태가 원천
  - ~~#7/#8 라이브러리~~ — 삭제(2026-08-08)
- **페이지네이션 UX**: 이 앱에는 무한 스크롤/페이지네이션이 필요한 목록 화면이 없다(라이브러리 목록 페이지네이션 요구사항은 2026-08-08 삭제). 남은 목록형 화면(홈 파일 목록, 학습 기록)은 전량 로컬 데이터라 해당없음.
- **오프라인 지원 범위**: 내 파일(홈 탭)은 분리 자체가 로컬 처리이므로 완전 오프라인으로 업로드~학습까지 가능(네트워크 불필요). ~~라이브러리(Tab2) 콘텐츠 캐싱~~ 요구사항은 삭제. 이 앱은 사실상 네트워크가 전혀 없어도 핵심 기능이 100% 동작하며, 유일한 네트워크 의존은 스토어 결제(구매/복원)와 선택적 통계 동기화뿐이다.
- **비용 구조 참고**: 문장 분리에 사용자당 과금되는 외부 API가 없으므로, 무료 사용자 수 증가가 API 비용 증가로 이어지지 않음(00_input.md 확정 사항). ~~라이브러리 콘텐츠 등록(CNN/BBC 수급) 고정 운영 비용~~ 항목은 별도 앱으로 이전되며 이 앱의 비용 구조에서 제외됐다 — 이 앱의 수익은 광고 노출(배너/전면) + 단발성 광고 제거 IAP(14,900원) 판매뿐이다.

## 스토어 매니저 전달 사항

### 베스트 스크린샷 후보 (우선순위순)

| 순위 | 화면 | 강조 메시지(카피 방향) |
|---|---|---|
| ★★★ 1 | #5 쉐도잉 학습 화면 (반복 도트 3/5 채워진 상태, 문장 텍스트 선명) | "듣고, 따라 말하고, 저절로 다음 문장" — 앱의 핵심 가치 |
| ★★★ 2 | #4 문장 분리 결과 확인 (파형+경계 핸들이 보이는 상태) | "타임스탬프? 몰라도 돼요, 자동으로 나눠드려요" — 참고 앱 대비 차별화 정면 승부 |
| ★★ 3 | #10 마이 홈 (스트릭 🔥4일 + 히트맵) | "매일의 습관이 눈에 보이게" — 재방문/꾸준함 소구 |
| ★★ 4 | #1 홈 (파일 목록, 진행률 바가 보이는 구성) | "내가 가진 영상/음성, 그대로 넣기만 하면" — 무료 핵심 가치 |
| ★ 5 | #0 온보딩 슬라이드 2 (핵심 루프 시연) | "내가 가진 영상, 그대로 넣기만 하면" |
| ★ 6 | 다크모드 스킨의 #5 학습화면 | 프리미엄 톤앤매너 강조용 대체 컷 |

**2026-08-08 갱신**: 옛 "#7 라이브러리 홈(CNN/BBC 배지)" 스크린샷 후보는 라이브러리 탭이 별도 앱으로 분리되며 제외했다. 이 앱 스크린샷에는 더 이상 CNN/BBC 출처 배지나 PRO 배지가 등장하지 않는다 — 순위 3~4를 마이 홈/홈 화면으로 대체했다.

- 스크린샷 캡처 시 문장 예시는 실제 학습 맥락처럼 보이는 자연스러운 영어 문장 사용 권장(예: 뉴스 인터뷰 발췌), Lorem ipsum류 금지.
- 참고 앱 대비 "더 적은 버튼, 더 큰 텍스트, 더 넓은 여백"이 스크린샷 상에서 즉각 대비되도록, 캡처본에는 UI 요소 수를 화면당 5개 이내로 유지한 상태를 사용할 것(학습 옵션 시트 등 2차 화면은 스크린샷에서 제외).
- ASO 키워드 후보(참고, app-store-optimization 스킬에서 상세화 예정): "쉐도잉", "영어 쉐도잉", "자동 문장 분리", "무자막 쉐도잉", "영어 회화 연습", "따라 말하기". ("AI 자막"은 AI/STT를 사용하지 않으므로 키워드·카피 어디에도 사용하지 않음 — 심사 반려 및 허위 광고 리스크 회피 목적.)
- 스토어 설명/스크린샷 카피에 "AI가 문장을 나눠준다", "AI 자막", "음성인식" 등 AI·STT를 암시하는 표현을 절대 사용하지 않는다 — 실제로는 기기 내 무음 감지 또는 사용자가 첨부한 자막 파일 매칭이며, 이 차이는 심사 대응(앱이 실제로 수행하는 처리 방식 소명) 시에도 그대로 사실대로 기술해야 한다.

## QA 엔지니어 전달 사항

- **접근성 검증 대상**: 위 "접근성 고려사항" 전체 항목 + 학습화면 원형 CTA의 동적 라벨 전환(듣기↔녹음)이 실제 VoiceOver/TalkBack에서 올바르게 발화되는지 우선 검증.
- **인터랙션 회귀 테스트 포인트**: (1) 문장 경계 드래그 후 자동 스냅이 무음구간을 정확히 인식하는지, (2) Hands-free 모드에서 문장 20개 연속 자동 진행 시 오디오 버퍼링/끊김 없는지, (3) 마이크 권한 거부 상태에서 학습 루프가 "듣기 전용"으로 정상 대체되는지, (4) 학습화면 내 광고 미노출 원칙이 실제 빌드에서 지켜지는지(광고 SDK 오초기화 방지).
- **로컬 처리 검증 (2026-08-05 추가)**: (5) 음성만 업로드 시 네트워크 요청이 단 하나도 발생하지 않는지(프록시/패킷 캡처로 확인 — 어떤 형태로든 외부 STT API 호출이 남아있지 않아야 함), (6) 영상+자막 업로드 시 같은 파일명 자막 자동 탐색이 정확히 동작하는지 및 자막 없이 진행 선택 시 무자막모드로 올바르게 전환되는지, (7) 무자막모드 학습화면에서 "한글 뜻 보기"/번역 관련 UI가 전부 숨겨지는지, (8) 자막 타임스탬프가 실제 발화와 어긋나 있어도 앱이 임의로 보정하지 않고 자막 그대로 표시하는지.
- **광고 제거 IAP 검증 (2026-08-08 추가)**: (9) 배너 광고가 홈 탭에서만 노출되고 마이 탭에는 애초에 렌더링되지 않는지, (10) 전면 광고가 세션 종료(#6) 화면 진입 시 정확히 1회만 트리거되고 학습 중(#5) "N문장마다" 같은 구간 트리거가 존재하지 않는지(코드 전수 검색으로 확인), (11) 광고 제거(#9) 구매 완료 즉시 배너/전면 광고가 앱 재시작 없이 둘 다 사라지는지, (12) 구매 복원(`restorePurchases`)이 새 기기/재설치 후에도 정상 동작하고 복원 실패 시 사용자에게 명확히 안내되는지, (13) 마이 홈(#10) 카드가 구매 상태에 따라 CTA/완료 배지 두 상태를 정확히 전환하는지.
- **2026-08-08 회귀 확인**: 옛 라이브러리 탭/구독/Paywall 관련 화면·라우트·PRO 배지·CNN/BBC 출처 배지가 이 앱 빌드에 전혀 남아있지 않은지(코드/네비게이션 전수 확인) — 남아있다면 별도 앱으로 분리하며 제거가 누락된 것.
- **크로스 플랫폼 시각 검증**: iOS/Android에서 Pretendard 폰트 렌더링 및 다크모드 대비값이 각각 WCAG AA를 충족하는지 실기기 확인 필요.

---

## 가정 사항 명시

- **문장 분리는 STT/서버 AI를 전혀 사용하지 않는다(2026-08-05 확정 사항, 본 문서에 전면 반영 완료)**: 음성만 업로드 시 로컬 무음 감지, 영상+자막 업로드 시 자막 파일 파싱만 사용. 이 결정에 따라 기존 문서에 있던 STT 이용 동의 바텀시트, "AI 분석 중" 화면, 신뢰도(confidence) 배지, AssemblyAI 등 벤더 언급을 전부 제거했다.
- 무음 감지 임계값(dB)과 최소 무음 길이(예: 400ms) 등 구체적 파라미터는 실측 튜닝이 필요한 값이며 app-developer와 협의 후 확정해야 한다 — 문서의 예시 수치는 초안 가정치.
- 로컬 처리이므로 "N분 정도 걸려요" 식의 네트워크 대기 문구는 의도적으로 배제했다. 다만 실기기에서 긴 파일(예: 60분)의 무음 감지 처리 시간이 사용자 체감상 얼마나 걸리는지는 app-developer의 실측 후 #3 화면의 진행률 표현 방식(예: 결정론적 % vs 인디케이터)을 재검토할 수 있다.
- SRT/VTT 인코딩(UTF-8 with/without BOM, CRLF 등) 예외 처리 범위는 app-developer와 별도 협의 필요.
- ~~CNN/BBC 콘텐츠 저작권·라이선스 이슈~~ 및 ~~요금제 가격(월 4,900원/연 39,000원) 시장 벤치마크 예시안~~ 가정 사항은 **2026-08-08자로 해당없음 처리**한다 — 둘 다 라이브러리 탭/정기구독 기능과 함께 별도 앱(English News Shadowing Lab)으로 이전됐다. 이 앱에 남은 유일한 가격 관련 항목은 광고 제거 IAP 14,900원이며, 이는 시장 벤치마크 예시가 아니라 **코드(`app_ko.arb`의 `adRemovalPrice`)에 이미 반영된 확정값**이다.
- 광고 제거 IAP(14,900원)의 스토어별 상품 ID(App Store Connect의 In-App Purchase 상품, Google Play Console의 관리형 상품) 등록 및 가격 등급 매핑은 store-manager 영역이며 본 문서 범위 밖이다.
